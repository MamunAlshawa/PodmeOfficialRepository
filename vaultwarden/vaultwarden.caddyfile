vaultwarden.example.com {

    # HTTP challenge ile otomatik SSL (Let's Encrypt)
    tls internal

    encode zstd gzip

    reverse_proxy localhost:11001 {
        header_up X-Real-IP {remote_host}
    }
}
