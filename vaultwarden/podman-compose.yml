services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vw.domain.tld"
    volumes:
      - podme-vaultwarden-vw-data:/data/
    ports:
      - 8000:80
volumes:
  podme-vaultwarden-vw-data:
